[workspace]
authors = ["Tom Soulanille <soul@prama.com>"]
channels = ["conda-forge"]
name = "ppln_ssfm"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]
run = "python main.py"

[dependencies]
python = ">=3.11"
numpy = ">=1.26"
matplotlib = ">=3.8"
scipy = ">=1.12"

[pypi-dependencies]
pyyaml = ">=6.0"
